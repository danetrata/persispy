"""
def neighbors(point)

def spectrum(point)

def heat_kernel(point)

def randomized_linear_scan(epsilon)
    points = [points,...]
    resample = []
    while(!points):
        randPoint = random.choice(x)
        x.remove(randPoint)
        if neighbors(x)
            s = spectrum(neighbors(x))
            if s > epsilon or not neighbors(x)
                resample.append(x)
"""



    
